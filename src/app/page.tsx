import ParticleSea from "@/components/ParticleSea";

export default function HomePage() {
  return (
    <main>
      <ParticleSea />
      {/* Your hero / nav can go here; the canvas is fixed behind it */}
    </main>
  );
}
